# Workflow Overengineering Review — 2026-06-27

担当: Claude (AI agent / claude-opus-4-8, 自動巡回タスク)
対象: Lictor, Praeforma, Conciliator

> **データ収集**: 今回は `gh` CLI が利用可能だったため、各リポの merged PR /
> commit 頻度を直接取得した (前回 2026-06-01 の間接評価の制約は解消)。
> 集計ウインドウ: 過去 1 ヶ月 = `since=2026-05-28T00:00:00Z`。

---

## Lictor

主目的: agent TUI CLI (Claude Code / Codex / ローカル LLM) を per-session で wrap し、セッション内 hook からホスト端末・キーストローク・Concordia 連携を駆動する sidecar
判定: 🟢 健全

### 6 観点

1. **消えたら困ること**:
   全 LUDIARS の AI セッションが Concordia へ session メタ・地の文 relay・質問カード・
   委託 prompt 注入・window title を送る経路を Lictor に依存している。消えると
   multi-agent 協調の端末側ブリッジ (1 セッション = 1 Lictor) が全滅する。
   1 行で書ける: "agent TUI を包んでセッション hook から端末/Concordia を駆動する sidecar"。

2. **既存リポへの集約可能性**:
   なし。Concordia は *多* セッションを束ねる中央、Lictor は *各* セッションに張り付く
   sidecar で責務が直交。前回 (06-01) の持ち越し「Lictor との責務境界文書化」は
   README の Providers 表・src 構成 (concordia.ts / ask-*.ts / harness-hook.ts) で
   実体として分離が維持されており、統合の必要は見られない。

3. **過去 1 ヶ月の PR 内訳 (本質 / リファクタ)**:
   merged PR 30 件 (#29–#62) / commit 36 件。
   - **本質機能 (feat/fix)**: #31, #33, #34, #35, #37, #39, #40, #41, #42, #43, #45,
     #46, #48, #49, #51, #52, #54, #56, #57, #59, #60, #62 ほか ≈ 23 件
     (askquestion プロトコル、transcript relay 再 pin、local LLM 拡張、force-exit
     猶予終了、harness-guard 注入 等)
   - **リファクタ**: #44 (local→gemma4-12 リネーム), #47 (provider を Famulus spawn へ
     載せ替え) = 2 件
   - **docs/chore/ci**: #29, #30, #32, #38, #58, #61 = 数件
   - 比率: 本質機能 >> リファクタ (23 : 2)。本質機能ドリブンが明確。

4. **重複している抽象**: なし。質問プロトコル (ask-marker / ask-json) は
   Lictor 内で 1 系統に統一されており、別名 2 重実装は見られない。

5. **設計再編リスク**: 低。PR はすべて具体的機能・バグ修正で、
   「リファクタのためのリファクタ」は確認されず。provider 抽象が
   claude/codex/gemma4-12 と増えているが、これは機能拡張であり過剰設計ではない。

6. **削除候補コンポーネント**: 特になし。`local` は #44 で gemma4-12 へリネーム後も
   alias として残置 (後方互換目的、妥当)。

### アクション
- 持ち越しの「Lictor↔Concordia 責務境界」は実体で維持確認済み → クローズ。
- 特段のリファクタ凍結・統合は不要。次回は通常巡回に戻す。

---

## Praeforma

主目的: プランナーが placeholder + 仕様を置き → デザイナーが art を当て → プログラマーが仕様通り実装する、を 1 プロジェクトとして繋ぐ「仕様↔実装連携」ツール
判定: 🟢 健全 (スコープ境界を継続監視)

### 6 観点

1. **消えたら困ること**:
   企画 placeholder → 仕様 → asset binding → 実装 のトレーサビリティ起点が消える。
   1 行で書ける: "placeholder/仕様/asset を 1 プロジェクトに束ねる雛形ツール"。
   ステータスは設計フェーズ寄りだが、PR #6/#7 で要件定義モード・ローカルモードが
   着地し実装が始まっている。

2. **既存リポへの集約可能性**:
   **要監視** (現時点は 🟢)。Thaleia (Th) が「Pf(仕様) と An(解析) を突合する単独
   サービス」として Pf から切り出された経緯がある (Pf→An 直リレーから方針転換)。
   PR #6 は "MUSA(Thaleia)経由 Anatomia グラフ" を *呼ぶ* 構成で、突合ロジックは
   Thaleia 側・Praeforma は authoring/binding 側、という責務分担が保たれている
   (合成であって重複ではない)。ただし Pf / Th / An の 3 者境界は曖昧化しやすく、
   次回も「Praeforma がやる範囲」を 1 行で言えるか再確認する。

3. **過去 1 ヶ月の PR 内訳 (本質 / リファクタ)**:
   merged PR 6 件 / commit 6 件。
   - **本質機能**: #6 (要件定義モード + Thaleia 経由 Anatomia グラフ),
     #7 (仕様書レビュー用ローカルモード SQLite) = 2 件
   - **docs/その他 (service scaffolding)**: #1, #2, #3 (favicon/icon 追加),
     #4 (favicon redesign) = 4 件
   - **リファクタ**: 0 件
   - 比率: chore 4 : 本質 2 だが、chore は web サービス立ち上げの favicon 整備で
     リファクタ churn ではない。直近 2 本が実機能で、トレンドは前進。

4. **重複している抽象**: なし。仕様スキーマは `spec/schema` に 1 系統。

5. **設計再編リスク**: 低〜中。リスクは「リファクタ過剰」ではなく
   **スコープ拡張** 側 (汎用ツールを謳い Unity/Web 両対応 + 要件定義 + ローカル
   モードと面が広がりつつある)。MVP を作らずフルセット方針は LUDIARS 規約通りだが、
   Thaleia/Anatomia との機能境界が広がると 🟠 候補に格上げ要検討。

6. **削除候補コンポーネント**: 現時点なし (実装が立ち上がったばかり)。

### アクション
- 次回 Praeforma レビュー時: Pf / Thaleia / Anatomia の 3 者責務境界を 1 行ずつで
  書き出し、突合ロジックが Pf 側に逆流していないか確認。
- スコープ肥大の予防として CLAUDE.md / spec に「Praeforma がやらないこと
  (= Thaleia/Anatomia に委ねること)」の 1 文明記を推奨。

---

## Conciliator

主目的: git が苦手な領域 (バイナリ Excel/Maya/Blender、未分解の作業) で作業衝突を「起きる前に」検知・意思確認し、起きた衝突はクレバーにマージ支援する (lock せず) サービス
判定: 🟡 要警戒 (本質機能 PR の停滞)

### 6 観点

1. **消えたら困ること**:
   バイナリアセットの衝突事前検知・3-way マージ支援・作業クレーム/意思確認が消える。
   1 行で書ける: "lock せずバイナリ/未分解作業の衝突を事前検知＋クレバーマージする協調基盤"。
   主目的は明確。

2. **既存リポへの集約可能性**:
   なし。README が明示する通り Concordia = *AI セッション* 協調、Conciliator =
   *ファイルと作業者* 協調で対象が異なる。Cernere 認証を借りるが識別のみで、
   衝突検知・3-way マージ (xlsx/.ma) は他リポに存在しない独自機能。統合候補なし。

3. **過去 1 ヶ月の PR 内訳 (本質 / リファクタ)**:
   merged PR 4 件 / commit 4 件 — **すべて favicon/icon 系 chore** (#1, #2, #3, #4)。
   - **本質機能**: 0 件
   - **リファクタ**: 0 件
   - **docs/その他**: 4 件 (favicon)
   - 実装本体 (src/ = agent / collision / merge / claims / risk / structure / watcher 等)
     は集計ウインドウ (2026-05-28) より前、v0.1/v1.0 design 期に着地済み。
   - リポ作成は 2026-05-16 (約 6 週前)、最終実コミット 2026-06-17、push 2026-06-21。
     → 3 ヶ月停止には該当せず 🔴 ではないが、直近 1 ヶ月の中核機能 PR が 0 で、
     表面的 (favicon) PR のみという停滞シグナルがある。
   - 現在 open の #5 (CI harness ゲート) は infra で本質機能ではない。

4. **重複している抽象**: なし。衝突検知/3-way マージは Conciliator 固有。

5. **設計再編リスク**: 低 (そもそも直近の変更が少ない)。
   リファクタ churn ではなく、むしろ **動きが止まっている** のが論点。

6. **削除候補コンポーネント**: 判断保留。src/ に広い構成 (server/agent/standalone +
   risk/structure/collision/merge) を持つが、実利用・実走検証の痕跡が今ウインドウに
   無いため、未使用コンポーネントの有無は本レビュー (10 分/件) では確証できない。

### アクション
- **要人間判断**: Conciliator は「中核実装が一段落して保守フェーズに入った (健全)」のか
  「立ち上げ後に放置されてパーク状態 (停滞)」のかが、PR 履歴だけでは区別できない。
  rubric 上の 🟡 トリガ (リファクタ PR > 本質 PR) には厳密には該当しない (リファクタ 0)
  が、本質機能 PR = 0 / chore のみという別シグナルで 🟡 と判定した。
- 次回までに確認すべきこと:
  1. Conciliator が実際に使われているか (warm server / 実利用ログの有無)。
  2. ロードマップ上「機能完成・保守」なら README に「現状: 機能凍結 (保守のみ)」を
     1 文明記して停滞を意図として顕在化させる。
  3. 放置パークなら、business-line roadmap での位置づけを再確認 (廃止 or 再開判断)。
- archive / 統合の **実行はしない** (本タスクの範囲外、TODO のみ)。

---

## 総評

| リポ | 判定 | 過去1ヶ月 merged PR | 本質 : リファクタ : chore | 傾向 |
|------|------|---------------------|---------------------------|------|
| Lictor | 🟢 健全 | 30 | 23 : 2 : ~5 | 本質機能ドリブンで活発 |
| Praeforma | 🟢 健全 | 6 | 2 : 0 : 4 | 設計→実装へ離陸中、scaffolding 期 |
| Conciliator | 🟡 要警戒 | 4 | 0 : 0 : 4 | 中核 PR 停滞、favicon のみ |

**全体の傾向 (4-6 件目)**:
- Lictor は前回の持ち越し (Concordia との責務境界) を実体で維持しつつ高頻度で
  本質機能を出しており、ワークフロー系の中で最も活発。
- Praeforma は「設計過剰」ではなく「設計フェーズから実装離陸の途中」。リスクは
  リファクタ過剰ではなくスコープ拡張 (Thaleia/Anatomia との境界) 側にある。
- Conciliator は 21 件で初めての非 🟢 判定。重複や他リポ吸収が理由ではなく、
  「中核機能の更新が直近 1 ヶ月止まり cosmetic PR のみ」という *活動停滞* が理由。
  rubric の 🟡 (リファクタ過多) とは別種のシグナルのため 要人間判断 を明記した。
- 今回 gh CLI 直接取得により、前回の間接評価の精度限界は解消された。

---

## 次回からどこを見るか
- 巡回再開: **Custos** (7番目) から (→ Custos / Actio / Schedula を想定)
- 持ち越し:
  - **Conciliator (要人間判断)**: 実利用の有無 / 保守フェーズか放置パークかの確定。
    保守なら README に「機能凍結 (保守のみ)」明記、放置なら roadmap 上で再開/廃止判断。
  - **Praeforma**: Pf / Thaleia / Anatomia の 3 者責務境界を 1 行ずつで再確認、
    spec/CLAUDE.md に「Praeforma がやらないこと」1 文を追記推奨。
  - Lictor の持ち越し (責務境界文書化) はクローズ済み。
