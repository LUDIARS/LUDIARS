# LUDIARS Services Progress — 2026-09-18

## 今回の更新

- `docs/data/services.json` と掲載承認の記録を境界として、2026-09-17 03:02 から 2026-09-18 03:02 (Asia/Tokyo) までの local `main` を集計した。
- 公開予定の Orbis / Tela を別枠とし、残る登録61リポジトリの日次集計では59件の checkout を確認、4リポジトリに4 commit があった。未マージ branch と worktree 内だけの変更は含めない。
- Praeforma の Unity Editor 向け仕様・指示・Scene 接続、AIFormat / Revisor の登録検査と検証手順を中心に更新が入った。
- 掲載承認済みの **Orbis / Tela** は「公開予定」として保持した。両者の local `main` は前回確認した commit から更新がなく、実装状況と残件を据え置いた。

## 公開予定のサービス

- **Orbis**：関心ごとにページをグラフで巡るデスクトップブラウザ。アセット / ツールに掲載。
- **Tela**：Pictor を使う再利用可能な C++ 宣言型ツール UI。UI / デザインシステムに掲載。

### Orbis の進捗

- **実装済み**：既存 Electron 版からの移行先として、Tela/Pictor の操作 UI と CEF/Chromium の Web 表示を組み合わせた Windows ネイティブホストを追加。戻る・進む・再読込と URL 表示を実装した。
- **残作業**：URL 入力、タブ、関心・履歴グラフ、既存データ、用途別設定、外部連携の移行。各 OS の配布とセキュリティ更新追従も完了条件に含む。既存版全体の移行完了ではない。
- **未確認**：IME・スクロール・DPI、終了時の子プロセス解放、待機 CPU/GPU などの実動作受入。本更新では実行していない。
- **根拠**：local `main` の `1873d3f`。前回確認した commit から追加 commit はなく、仕様と移行・配布タスクに記録された実装状況・残件を継承した。

### Tela の進捗

- **実装済み**：宣言型 UI ランタイム、入力領域・ジェスチャー所有権、図形描画と対象追従ラベル、Pictor 描画アダプタ、Windows オーバーレイ、Unity Editor 連携と遷移編集サンプル、macOS AppKit オーバーレイアダプタを保持している。仕様書カード、シーン仮配置、ドメイン関係図の表示にも対応している。
- **残作業・未確認**：仕様書ビューの先頭カードと操作パネルの重なり解消、単体表示と Unity Scene ビューでの実画面受入、関係図の可読性、複数モニタ / DPI、再接続、待機 CPU とネイティブハンドルを確認する。Mac SDK でのネイティブビルドと実機受入も残る。
- **根拠**：local `main` の `cb09744`。前回確認した commit から追加 commit はなく、仕様と残件を継承した。今回、テストや実画面確認は再実行していない。

2件の進捗は掲載承認に基づく機能単位の説明であり、61件・4 commit の日次集計には含めない。全体の完了条件に対する達成率を算定する根拠がないため、数値の完成度は引き続き未計測。

## 進んだこと

### Unity Editor での仕様確認と指示保存

**Praeforma（1 commit）**：Unity Editor に仕様一覧と指示保存を追加し、選択した Unity オブジェクトを指示に添えられるようにした。送信結果が不明な場合も同じ要求として再送でき、下書きを Editor の domain reload 後も保持する。Tela への Scene 接続は任意パッケージへ分離した。実際の Editor、HTTP 応答、Scene overlay での操作は未確認。

### 検証台帳と非コード変更の登録検査

**AIFormat（1 commit）**：公開履歴移行について、既存検査の Augur 台帳、プログラム層宣言、検証手順を追加した。台帳への登録であり、今回の集計作業が検査実行や合格を確認したものではない。

**Revisor（1 commit）**：Augur 台帳がある非コード変更では、コードドメイン不要と明示された計画に限り登録済み検査を選べるようにした。コード変更や分類不明で対象ドメインがない場合は、引き続き実行不能として止める。

### 日報

**LUDIARS（1 commit）**：2026-09-17 のサービスダッシュボード日報と snapshot catalog を追加した。

## 確認した main の変更根拠

| リポジトリ | 件数 | 対象期間の最新 commit |
|---|---:|---|
| LUDIARS | 1 | [b92371d1](https://github.com/LUDIARS/LUDIARS/commit/b92371d1) |
| AIFormat | 1 | [ec18386c](https://github.com/LUDIARS/AIFormat/commit/ec18386c) |
| Praeforma | 1 | [1bd466fb](https://github.com/LUDIARS/Praeforma/commit/1bd466fb) |
| Revisor | 1 | [191bb7e5](https://github.com/LUDIARS/Revisor/commit/191bb7e5) |

Orbis / Tela は公開予定の別枠で local `main` を確認し、対象期間の追加 commit はなかった。ローカル main の確認であり、各 commit の稼働環境への反映を保証するものではない。

## 完成度と未確認事項

- commit 数は活動量であり、完成条件の達成率ではない。完成度を再評価する定量的根拠は得られていないため、直前のスナップショットにある **72%** と公開対象のリポジトリ別完成度を継承した。72% は過去の評価値で、今回の61件を再集計した割合ではない。
- 未計測のプロジェクトに推測の完成度は追加しない。
- **ErgoDLLs / Omnipotens** は local checkout が見つからず、数値を取得できず。変更なしとは扱わない。残る55リポジトリは対象期間の main に commit がなかった。
- 本更新ではテスト・起動・デプロイを実施していない。実装済みと動作確認済みを区別する。
