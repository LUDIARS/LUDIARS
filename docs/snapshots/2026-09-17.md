# LUDIARS Services Progress — 2026-09-17

## 今回の更新

- `docs/data/services.json` と掲載承認の記録を境界として、2026-09-16 03:03 から 2026-09-17 03:03 (Asia/Tokyo) までの local `main` を集計した。
- 登録63リポジトリは GitHub visibility がすべて `PUBLIC` だった。正本の明示条件に従い Orbis / Tela は引き続き「公開予定」の別枠とし、残る61リポジトリの日次集計では59件の checkout を確認、7リポジトリに15 commit があった。未マージ branch と worktree 内だけの変更は含めない。
- Praeforma と Tela の仕様書・シーン・ドメイン関係図表示、Augur / Concordia / Revisor のテスト証跡と審査報告、GitHub 履歴移行の handoff を中心に更新が入った。
- 掲載承認済みの **Orbis / Tela** は「公開予定」として保持した。Orbis の local `main` は前回から更新なし。Tela は8 commit で表示機能が進み、実画面受入などの残件も更新した。

## 公開予定のサービス

- **Orbis**：関心ごとにページをグラフで巡るデスクトップブラウザ。アセット / ツールに掲載。
- **Tela**：Pictor を使う再利用可能な C++ 宣言型ツール UI。UI / デザインシステムに掲載。

GitHub visibility は今回の確認時点で両者とも `PUBLIC` だったが、日報正本の明示条件に従い、カタログ上の扱いは「公開予定」のまま維持した。

### Orbis の進捗

- **実装済み**：既存 Electron 版からの移行先として、Tela/Pictor の操作 UI と CEF/Chromium の Web 表示を組み合わせた Windows ネイティブホストを追加。戻る・進む・再読込と URL 表示を実装した。
- **残作業**：URL 入力、タブ、関心・履歴グラフ、既存データ、用途別設定、外部連携の移行。各 OS の配布とセキュリティ更新追従も完了条件に含む。既存版全体の移行完了ではない。
- **未確認**：IME・スクロール・DPI、終了時の子プロセス解放、待機 CPU/GPU などの実動作受入。本更新では実行していない。
- **根拠**：local `main` の `1873d3f`。前回確認した commit から追加 commit はなく、仕様と移行・配布タスクに記録された実装状況・残件を継承した。

### Tela の進捗

- **今回の実装**：Praeforma が書き出す仕様書カード、シーン仮配置、ドメイン関係図を Windows のオーバーレイまたは Tela 自身のビューで描けるようにした。グループ / 分類の表示切り替え、対象ウインドウ隣への等倍配置、文字サイズ、複数行テキスト、円形配置、有向関係の矢じりに対応した。
- **既存の実装**：宣言型 UI ランタイム、入力領域・ジェスチャー所有権、図形描画と対象追従ラベル、Pictor 描画アダプタ、Windows オーバーレイ、Unity Editor 連携と遷移編集サンプル、macOS AppKit オーバーレイアダプタを保持している。
- **残作業・未確認**：仕様書ビューの先頭カードと操作パネルの重なり解消、単体表示と Unity Scene ビューでの実画面受入、関係図の可読性、複数モニタ / DPI、再接続、待機 CPU とネイティブハンドルを確認する。Mac SDK でのネイティブビルドと実機受入も残る。
- **根拠**：local `main` の `cb09744`。対象期間の8 commit と `spec/feature` / `spec/tasks` に記録された未確認事項を照合した。今回、テストや実画面確認は再実行していない。

2件の進捗は掲載承認に基づく機能単位の説明であり、61件・15 commit の日次集計には含めない。全体の完了条件に対する達成率を算定する根拠がないため、数値の完成度は引き続き未計測。

## 進んだこと

### 仕様・シーン・関係図の可視化

**Praeforma（6 commits）**：複数シーンの仮配置を重ねて切り替える編集面と Tela 向け書き出しを追加した。構造化仕様を軸別の図にし、ドメイン関係図の共有配置・円形配置と Tela 向け出力も整備した。

### テスト証跡と審査報告

**Concordia / Augur / Revisor（6 commits）**：必須設定のあるプロジェクトへ DDD / 契約の実装手順を注入し、既存テストの登録と Praeforma 証跡化を追加した。Revisor は失敗理由と所見を日本語化し、計画済みテスト、動作証跡、体験証跡を審査報告へ載せるようにした。

**Anatomia（1 commit）**：DDD ゲート向けにプロダクト UX draft と各ドメインの仕様参照を追加した。

### 公開履歴移行と日報

**AIFormat（1 commit）**：GitHub 履歴移行について、Revisor handoff の生成と公開・復旧の完了検証を追加した。

**LUDIARS（1 commit）**：2026-09-16 のサービスダッシュボード日報と snapshot catalog を追加した。

## 確認した main の変更根拠

| リポジトリ | 件数 | 対象期間の最新 commit |
|---|---:|---|
| LUDIARS | 1 | [11ba9245](https://github.com/LUDIARS/LUDIARS/commit/11ba9245) |
| AIFormat | 1 | [df1424f1](https://github.com/LUDIARS/AIFormat/commit/df1424f1) |
| Concordia | 2 | [a77b9cd1](https://github.com/LUDIARS/Concordia/commit/a77b9cd1) |
| Praeforma | 6 | [05155e17](https://github.com/LUDIARS/Praeforma/commit/05155e17) |
| Augur | 1 | [a3611660](https://github.com/LUDIARS/Augur/commit/a3611660) |
| Anatomia | 1 | [db9bcb09](https://github.com/LUDIARS/Anatomia/commit/db9bcb09) |
| Revisor | 3 | [403bcb35](https://github.com/LUDIARS/Revisor/commit/403bcb35) |

公開予定の別枠では Tela の8 commit（最新 `cb09744`）を確認した。ローカル main の確認であり、各 commit の稼働環境への反映を保証するものではない。

## 完成度と未確認事項

- commit 数は活動量であり、完成条件の達成率ではない。完成度を再評価する定量的根拠は得られていないため、直前のスナップショットにある **72%** と公開対象のリポジトリ別完成度を継承した。72% は過去の評価値で、今回の61件を再集計した割合ではない。
- 未計測のプロジェクトに推測の完成度は追加しない。
- **ErgoDLLs / Omnipotens** は local checkout が見つからず、数値を取得できず。変更なしとは扱わない。残る52リポジトリは対象期間の main に commit がなかった。
- 本更新ではテスト・起動・デプロイを実施していない。実装済みと動作確認済みを区別する。
