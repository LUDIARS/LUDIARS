# LUDIARS サービス状況スナップショット (2026-04-27)

LUDIARS org の現役 31 リポジトリ (services.json に登録済の 25 + 新規・
未登録 6) を、ローカルクローンと GitHub API から解析した一覧です。
完成度は主観的な概算で、実装量・運用実績・残タスクの見え方を根拠に
付けています。前週 (2026-04-24) からの主な変化は冒頭サマリと「前週との
比較」節にまとめます。

## 全体サマリ

| カテゴリ | リポジトリ | 完成度 |
|---|---|---|
| **メタ/インフラ** | [LUDIARS](#ludiars-meta) | 72% |
| | [infra](#infra) | 90% |
| | [Codex](#codex) | 45% |
| **コアエンジン / SDK** | [Ars](#ars) | 60% |
| | [Ergo](#ergo) | 78% |
| | [Pictor](#pictor) | 70% |
| | [AIFormat](#aiformat) | 80% |
| **認証 / 通知** | [Cernere](#cernere) | 86% |
| | [Nuntius](#nuntius) | 75% |
| **スケジュール / タスク** | [Actio / Schedula](#actio--schedula) | 86% |
| | [Actio-PublicModules](#actio-publicmodules) | 40% |
| | [Actio-SchoolModules](#actio-schoolmodules) | 65% |
| | [Calicula](#calicula) | 60% |
| | [Discutere](#discutere) | 75% |
| **ゲーム / ネットワーク** | [AdventureCube](#adventurecube) | 70% |
| | [Synergos](#synergos) | 55% |
| | [Tessera](#tessera) | 10% |
| **アセット / ツール** | [Curare](#curare) | 45% |
| | [Clio](#clio) | 20% |
| | [Signum](#signum) | 25% |
| | [Imperativus](#imperativus) | 75% |
| | [Iter](#iter) | 5% |
| **Ars プラグイン** | [ars-module](#ars-module) | 35% |
| | [ars-musa](#ars-musa) | 25% |
| | [ars-platform-plugin](#ars-platform-plugin) | 55% |
| **未登録 (services.json 追加要)** | [Custos](#custos) | 30% |
| | [All-In-OneTest](#all-in-onetest) | 70% |
| | [Voluptas](#voluptas) | 18% |
| | [Ludus](#ludus) | 10% |
| | [Educatus](#educatus) | 3% |
| | [ErgoDLLs](#ergodlls) | 0% |

**加重平均 (services.json 登録 25 リポ): ≈ 56%** (前週 55% → +1%)

---

## メタ / インフラ

### LUDIARS (meta)

- **概要**: LUDIARS 組織全体のメタリポジトリ。GitHub Pages
  ダッシュボード + All-in-One Docker Compose + 自動ホスト統合 CI を
  ホスト。今週 self-hosted runner 用 integration ワークフローを main
  にマージ。
- **完成度**: 72% (+2) — 統合 CI の self-host 設定 + Windows / macOS
  ランナー対応がコード反映済。残るは All-In-OneTest (private) との
  連携とデプロイ自動化。
- **完成している機能**:
  - All-in-One Docker Compose (Cernere / Actio / Nuntius / Imperativus)
  - GitHub Actions integration ワークフロー (self-hosted runner)
  - `scripts/integration-{up,down,wait,test,logs}.{sh,ps1}` (Windows 対応)
  - `compose/all-in-one.yaml` 統一サービス定義
  - GitHub Pages ダッシュボード (`docs/index.html` + `docs/data/*.json`)
  - 週次 snapshots 自動更新 routine
- **未完成 / 次のタスク**:
  - self-hosted runner の本番登録・運用検証
  - クロスサービス認証フロー自動テスト
  - 分散トレーシング・監視統合
  - デプロイ自動化 (本番・ステージング)
- **主要依存**: Docker Compose v2 / GitHub Actions / Pages

### infra

- 前週から変化なし (90%)。本番利用可、PostgreSQL 17 / Redis 7 /
  MinIO / MailHog の 4 サービスを Compose ワンステップで提供。
  メトリクス監視 (Prometheus / Grafana)・バックアップ自動化が残課題。

### Codex

- 前週から変化なし (45%)。v0.10 設計 + M0-M9 実装完了
  (core / crypto / state / consensus / sync / light / node / demo / FFI)。
  プロダクション硬化と Tessera / Curare 統合が次フェーズ。

---

## コアエンジン / SDK

### Ars

- 前週から変化なし (60%)。Tauri App 版ノードエディタとコア機能は完成。
  Axum Web 版・`ars-collab` は引き続き実装待ち。

### Ergo

- **概要**: C++17 モジュール群。AdventureCube / Pictor を含む全アプリ
  共通の入力・音響・パーティクル・UI 基盤。
- **完成度**: 78% (+3) — 今週 4 つの主要追加。
- **完成している機能 (今週追加)**:
  - **`ergo_custos`**: in-process HTTP/1.0 サーバ + 最小 PNG エンコーダ。
    ホスト ffmpeg gdigrab + nut-js を bypass し、アプリ自身が swapchain
    から直接 RGBA8 → PNG を返す。preview-status システムモジュール
    として登録 (`spec/module/custos.md`、9 tests)
  - **benchmarks/**: 外部依存なしの header-only ベンチマークハーネス。
    CPU パーティクル best-of-3 で ~20 ns/particle/frame ベースライン
  - **ergo_gpu_particle**: CPU fallback の shape sampling +
    burst cycles/interval 対応
  - **ergo_terrain**: Field カテゴリ拡張 (sand / water / lava / wood /
    ice parity)
  - **ergo_placer**: skill catalog 49 entries に拡張
- **未完成 / 次のタスク**:
  - GPU Compute Shader 統合 (Shuriken 相当)
  - terrain Stage 構成の本格活用 (AdventureCube 側との連携)
  - プロファイリング・最適化
- **主要依存**: Pictor / FMOD Core fallback。main 一本運用継続。

### Pictor

- **概要**: データ駆動型 Vulkan / WebGL レンダリング基盤。
- **完成度**: 70% (+5) — 今週 4 つの主要コミット。
- **完成している機能 (今週追加)**:
  - **headless test suite** (PICTOR_BUILD_TESTS=ON 既定):
    frame_allocator / radix_sort / culling / pixel_text / fps_baseline。
    GPU フリーで CTest <4s
  - **hologram シェーダ**: SkillBox / Equipment 用 fresnel + scan-lines。
    既存 SceneUBO/InstanceData レイアウトと互換 (drop-in swap)
  - **WebGL frustum culling**: Gribb/Hartmann plane extraction +
    AABB p-vertex test、`WebGLFrameStats.culled_objects` 公開
  - **MaterialDesc ↔ JSON シリアライザ + tools/material-editor**
  - **mobile lifecycle API**: pause/resume/surface-lost + thermal
    auto-downgrade
  - **Android NDK 計画書** (`docs/android-build.md`): Phase 1-3
    (libpictor.a → headless emulator → 最小 APK Vulkan clear) と
    8 項目の未決事項を整理
- **未完成 / 次のタスク**:
  - Android (NDK) 実装着手 (AVX2 ARM fallback / AndroidSurfaceProvider)
  - ストリーミング LOD・遅延ロード
  - マテリアルエディタの Ars 連携
  - GaussianBlurEffect (GPU) ベース大領域 glow 高速化

### AIFormat

- 前週から変化なし (80%)。CLAUDE.md コマンド・スキル・spec 構成・
  Cernere 認証規約・Drizzle ORM 戦略は LUDIARS 全体で運用中。

---

## 認証 / 通知

### Cernere

- **概要**: LUDIARS 全サービスの認証 + WebSocket リレー基盤。
- **完成度**: 86% (+1) — 今週セキュリティ硬化と仕様文書整備。
- **完成している機能 (今週追加)**:
  - **WS authz 4 層防御** (#49 / #72): `commands.dispatch` に Layer 2-3
    ガード (Redis セッション + state==logged_in)、`operation_logs`
    silent catch を console.error 化、`resolveProjectWsAuth` で
    projectKey × DB key 整合性チェックを upgrade 段階に追加
  - **dev logging + project conn registry + user-project row init**
  - **spec/security_design.md §3.2-3.4** を実装に合わせて更新
  - **user-auth-project-open フロー** (mermaid 図付き spec 追加)
- **未完成 / 次のタスク**:
  - `service-adapter` 拡張 (Nuntius が今週 PeerAdapter 取り込み済)
  - 操作ログ監査 UI フロントエンド
  - セッション管理スケーリングのパフォーマンスチューニング

### Nuntius

- **概要**: LUDIARS 統合通知・メッセージング基盤。
- **完成度**: 75% (+5) — 今週 2 つの主要 PR が merge。
- **完成している機能 (今週追加)**:
  - **service-adapter**: Cernere PeerAdapter 組込み + ping-pong (#8)
  - **配信履歴 API + チャネル別フォーマッタ** (#9)
  - **channel_credentials AES-256-GCM 暗号化保存**:
    `NUNTIUS_ENCRYPTION_KEY` (Infisical 管理)、エンベロープ形式 v=1。
    env 上の `SLACK_/DISCORD_/LINE_/ALEXA_/AWS_/SMS_` 系を全て削除
- **未完成 / 次のタスク**:
  - Email / SMS チャネル実装の最終化
  - Alexa Proactive Events 統合
  - Imperativus 音声チャネル連携

---

## スケジュール / タスク

### Actio / Schedula

- **完成度**: 86% (+1) — `spec/data-scheme.md` 追加。Actio DB と
  Cernere user_data の責務分離が文書化された。
- 残課題は前週から変わらず: `ctx.modules.invoke` 全モジュール間通信、
  Discutere 分離後の MACHINA レガシー削除完全化、外部モジュール npm
  動的ロード、フロントエンド統合。

### Actio-PublicModules

- 前週から変化なし (40%)。`module/curriculum-scheduler` Phase 2.0
  実装が最先行で、aggregate CI / 他モジュール公開化が残。

### Actio-SchoolModules

- 前週から変化なし (65%)。`curriculum-scheduler` Phase 2.0 完了済、
  Phase 2.1-2.3 (holiday 連携 / 教室割当 / Calendar sync) と
  Schedula `ctx.modules.invoke` 待ち。

### Calicula

- 前週から変化なし (60%)。Phase 1 + Phase 1.5 完了、Phase 2.0-2.4 残。
  Repository 抽象化 / 内蔵 HTTP サーバ / curriculum JSON Schema v1
  固定が次工程。

### Discutere

- 前週から変化なし (75%)。Slack / Discord chat-to-task コアと
  チャンネルモード状態管理は本番投入可。JIRA / Asana リレーアダプタと
  オンメモリ → Redis 移行が残。

---

## ゲーム / ネットワーク

### AdventureCube

- **概要**: Vulkan + Pictor + Ergo の BPM アクション 3D ゲーム。
- **完成度**: 70% (+5) — 戦闘層が大刷新。
- **完成している機能 (今週追加)**:
  - **Skill→ActiveObject→Action 戦闘層**: `src/combat/{tag,action,
    targetable,active_object,active_object_runtime,enemy_targetable,
    enemy_ai}.*` フルチェーン。VFX フック (`IActiveObjectObserver`) と
    IAction テンプレート (Damage / Heal / Knockback / Status: Burn /
    Slow / Stun / Bleed)
  - **`SkillObject` + `IActionTemplate` + `TemplateSkill` +
    `SkillObjectCatalog`**: per-skill spawn template、`FireContext`
    で owner_attack_damage + cube offset を相手に伝達
  - **Shield Block 体力モデル (HP 廃止)**: `PlayerShields` +
    `PlayerTargetable`。1 hit = 1 shield 消費 (damage 値無視)、
    0.6s i-frames で multi-hit 防止。`HealAction` が shield を回復
  - **iso UI HUD**: `[#] [#] [#]` の Shield slot 表示 + colour
  - **drops**: 倒した敵から item drop パイプライン
  - **起動時 Melee 前方 3 マス + Shield 3 装備の既定構成**
- **未完成 / 次のタスク**:
  - SkillBox 抽選・配置・ロジック完成
  - 装備 UI Vulkan 描画経路への本結線 (FrameLibrary RGBA8 → texture)
  - ステージ・敵 AI・ボスパターン拡充
  - VFX 連携 (アフターイメージ・エフェクトチェイン)

### Synergos

- 前週から変化なし (55%)。171 tests + Bitswap v2 + CatalogSyncService
  + auto-update 経路完成。残るは TURN 完遂 (Issue #23) と
  GUI Monitor / Transfer Manager UI、ars-plugin EventBus ↔ IPC Bridge。

### Tessera

- 前週から変化なし (10%)。設計書 v0.7 のみ。Synergos TURN 完遂と
  Codex 独立化を待っている状態。

---

## アセット / ツール

### Curare

- 前週から変化なし (45%)。Phase 1 MVP 開発中。Asset Service の基本
  ロジックと API/DB 設計は完成、Workflow / Pipeline / Project Service
  と Web UI が残。

### Clio

- 前週から変化なし (20%)。設計書のみ。実装着手待ち。

### Signum

- 前週から変化なし (25%)。Web 基盤 + PNG → SVG 基本トレース完成。
  JPEG/WebP 対応 + アップロード UI が次フェーズ。

### Imperativus

- 前週から変化なし (75%)。WebRTC + STT + Cernere 認証 + Admin UI
  完成。Node ↔ Python IPC 最適化と本番デプロイ最適化が残。

### Iter

- 前週から変化なし (5%)。`corespec.md` 9 フェーズ計画のみ。

---

## Ars プラグイン

### ars-module

- 前週から変化なし (35%)。Git / GitHub API / Auth / Collab の
  Rust モジュール群が commit 済、README とテストが薄い。

### ars-musa

- 前週から変化なし (25%)。Melpomene / Terpsichore / Thaleia の
  3 Tauri アプリ scaffold 段階。

### ars-platform-plugin

- 前週から変化なし (55%)。Data Organizer / Resource Depot / SpecTracker
  の API 仕様完成 + SpecTracker 動作可。本実装統合が次工程。

---

## 未登録リポジトリ (services.json への追加が必要)

GitHub には存在するが `docs/data/services.json` に未登録のリポジトリ。
ダッシュボードへ反映するには `services.json` の更新が必要。

### Custos

- **概要**: LUDIARS 遠隔テストランナー (新設 2026-04-25)。Hono +
  ws + Electron でブラウザから build/run/kill を駆動、ログとテスト結果を
  見ながらバーチャルキーで操作。`config/apps.json` で対象を宣言。
- **完成度**: 30% — Phase 1 MVP + Phase 2 主要機能 (Cernere 認証 +
  WebRTC capture + Android adb input) 着手段階。3 日間で 14 コミット
  という非常に高い活動度。
- **完成している機能**:
  - REST: build / run / test / kill / status (zod 検証済)
  - WS: ログ・状態変化のストリーミング、仮想キー入力
  - 4-tab Frontend (VIEW / LOG / TEST + Capture 制御)
  - Frontend (4649) / Backend (7676) split + 単一 Hono dual-port
  - WebRTC + werift (H.264 RTP) + NVENC + screenshot API
  - ergo_custos クライアント連携 (in-process bridge)
  - Cernere 認証 (anonymous mode 既定)
  - 8 個のテストファイル (auth / config / h264 / registry / runner /
    scale / screenshot / stream-prefs)
- **未完成 / 次のタスク**:
  - Android emulator 完全統合 (adb input keyevent)
  - Cernere service-adapter 経由の本番認証
  - WebRTC pixel-perfect ストリーミング検証
  - apps.json プリセットの拡充 (Pictor mobile demo / AdventureCube 以外)
- **主要依存**: hono / werift / electron / Cernere / ergo_custos

### All-In-OneTest

- **概要**: Cernere / Actio / Nuntius / Imperativus を submodule で
  取り込んだ**内部統合テストハーネス** (private)。`LUDIARS/LUDIARS`
  公開用 CI と切り分け、cross-repo 検証はこちらで回す。
- **完成度**: 70% — Compose / scripts / submodule 構成は本番運用可。
  self-hosted runner 登録待ち。
- **完成している機能**:
  - 4 サービス submodule の `services/` 配下管理
  - `compose/all-in-one.yaml` + `scripts/integration-{up,down,wait,
    test,logs}.{sh,ps1}` (Windows + macOS + Linux)
  - `refresh_submodules=true` で `origin/main` 追従モード
  - GitHub Actions integration ワークフロー
- **未完成 / 次のタスク**:
  - self-hosted runner の組織登録・運用 (memory にも残課題で記載)
  - 失敗時のサービスログ集約・分析
  - シナリオテスト追加 (chat → notification → task の cross-flow)

### Voluptas

- **概要**: Player Profile Backend。OpenID Connect (PKCE) ID 発行 +
  ゲームプレイロギング + 嗜好分析エンジン。3 パターン分類 (MTG
  psychographics / Caillois 4 categories / Berne life scripts) で
  12 次元ベクトル生成。
- **完成度**: 18% — README + 設計 + 初期 Express バックエンド +
  React 19 SPA scaffold。Bartle taxonomy → 3-pattern hobby
  classification への置き換え PR (#3) で分析エンジン雛形は完成、
  実プレイログ収集はこれから。
- **完成している機能**:
  - OpenID Connect (PKCE) + Google / Discord / Steam 連携設計
  - PostgreSQL + Redis スキーマ
  - JWT (RS256) + JWKS
  - migrations 002 で classification_data + subtype_data 列
  - `/api/v1/analysis/schema` エンドポイント
  - AnalysisPage 雛形 (per-pattern radar + classification summary)
- **未完成 / 次のタスク**:
  - イベント収集 API 本実装
  - アンケート基盤 UI
  - Cernere 連携への置き換え (現在は独自 OIDC)
  - 8 軸 → 12 軸ベクトルの妥当性検証

### Ludus

- **概要**: モジュール設計テンプレート集 (`module_template.md` +
  CLAUDE.md)。「概要 / カテゴリ / 所属ドメイン / データ / 依存 / 変数 /
  作業」の 7 項目フォーマットを Ergo / Ars 実装の上流に置く位置付け。
- **完成度**: 10% — テンプレート + ルールのみ。`spec/` ディレクトリは
  空。実モジュール定義の蓄積はこれから。
- **未完成 / 次のタスク**:
  - 各モジュール定義書の `spec/module/` への蓄積
  - Ergo / Ars コードジェネレータと定義書のマッピング
  - テスト項目の Lint ルール化

### Educatus

- **概要**: 不明 (README は `# Educatus` のみ、2026-04-07 作成以降
  push なし)。LUDIARS の教育系プロジェクト用と推測。
- **完成度**: 3% — placeholder。
- **次のタスク**: 設計書執筆。

### ErgoDLLs

- **概要**: Ergo の DLL 配布用と推測 (リポジトリ完全に空、
  2026-02-24 作成以降 push なし)。
- **完成度**: 0% — リポジトリ空。
- **次のタスク**: 用途確定 + 初期 commit (もしくは archive)。

---

## 横断的観察

### 依存の層構造

```
  [ゲーム層]   AdventureCube ── Tessera        Voluptas (Player profile)
                     │            │                │
  [エンジン層]  Ergo ──── Pictor ──── Synergos ──── Codex
                     │            │          │
  [テスト基盤]  Custos ── ergo_custos ── All-In-OneTest
                     │
  [エディタ層]  Ars ── ars-musa / ars-platform-plugin / ars-module
                     │
  [サービス層]  Actio ── Discutere ── Imperativus ── Signum ── Curare ── Calicula
                     │
  [基盤層]      Cernere ── Nuntius ── infra ── AIFormat ── LUDIARS (meta)

  [設計層]      Ludus (モジュールテンプレート) ── Educatus (?)
```

- **テスト基盤レイヤーの新設**: Custos + ergo_custos + All-In-OneTest が
  この週で実線でつながった。`AdventureCube` / `Pictor mobile demo` を
  ホスト ffmpeg なしで遠隔操作・スクリーンショット取得できる
- **インフラ基盤 (infra / Cernere / Nuntius / AIFormat)** は今週も
  86-90% で本番品質。Cernere の WS authz 硬化 + Nuntius の
  service-adapter 取り込みで連携経路が太くなった
- **エンジン系 (Ergo / Pictor)** が今週もっとも進展 (Ergo 75→78、
  Pictor 65→70)。AdventureCube のドッグフード経由で必要機能を順次追加
- **ゲーム層 (AdventureCube 65→70)** は戦闘層を Skill→ActiveObject→
  Action に再構築、Shield Block で HP 概念を廃止した非可逆変更

### 完成度別分布

- **80%+ (本番運用可)**: infra, Cernere, Actio/Schedula, AIFormat
- **70-79% (主要機能完成)**: Ergo, Nuntius, Pictor, AdventureCube,
  Discutere, Imperativus, LUDIARS (meta), All-In-OneTest
- **60-69%**: Actio-SchoolModules, Calicula, Ars
- **40-59% (MVP 開発中)**: Synergos, ars-platform-plugin, Curare,
  Codex, Actio-PublicModules
- **20-39%**: ars-module, Custos, Signum, ars-musa, Clio
- **10-19% (設計段階)**: Voluptas, Tessera, Ludus
- **0-9% (空 / 設計書のみ)**: Iter, Educatus, ErgoDLLs

### 直近の活動 (2026-04-24 → 2026-04-27)

- **Custos** が初期 MVP → Phase 2 (WebRTC + Cernere + Android) を 14
  コミットで一気に押し上げ
- **AdventureCube 戦闘層** Skill→ActiveObject→Action + Shield Block
  (HP 廃止)
- **Pictor** headless test suite + hologram シェーダ + WebGL frustum
  culling + MaterialDesc JSON + Android NDK 計画
- **Ergo** ergo_custos 新設 + benchmark harness + GPU particle CPU
  fallback + terrain Field 拡張
- **Cernere** WS authz 4 層防御 + project conn registry
- **Nuntius** Cernere service-adapter PeerAdapter + 配信履歴 API +
  チャネル別フォーマッタ + AES-256-GCM channel credentials
- **LUDIARS metarepo** integration CI ワークフロー main マージ

### 次フェーズで解けるとインパクトが大きい項目

1. **Custos ↔ AdventureCube / Pictor mobile demo の往復ループ確立** →
   開発検証サイクルが秒単位に短縮
2. **Synergos TURN 完遂** (Issue #23) → Tessera 実装ブロック解除
3. **Pictor Android NDK Phase 1-3 実装** → モバイルゲーム配信が現実的に
4. **services.json への 6 リポ追加** → ダッシュボード網羅率 100%

---

## 前週との比較

### 完成度が動いたリポジトリ (±1% 以上)

| リポジトリ | 2026-04-24 | 2026-04-27 | 差分 | 主な要因 |
|---|---|---|---|---|
| LUDIARS (meta) | 70 | 72 | +2 | self-hosted runner integration ワークフロー main マージ |
| Ergo | 75 | 78 | +3 | ergo_custos / benchmarks / terrain Field / skill catalog 49 |
| Pictor | 65 | 70 | +5 | headless test + hologram + WebGL culling + MaterialDesc + mobile lifecycle |
| Cernere | 85 | 86 | +1 | WS authz 4 層防御 + project conn registry |
| Nuntius | 70 | 75 | +5 | service-adapter + 配信履歴 API + AES-256-GCM credentials |
| Actio / Schedula | 85 | 86 | +1 | data-scheme.md spec 追加 |
| AdventureCube | 65 | 70 | +5 | Skill→ActiveObject→Action + Shield Block (HP 廃止) |

### 新規追加 (前週は記載なし)

| リポジトリ | 完成度 | 備考 |
|---|---|---|
| Custos | 30% | 2026-04-25 新設、テストランナー、3 日で 14 コミット |
| All-In-OneTest | 70% | private 内部 CI、submodule 構成 |
| Voluptas | 18% | Player Profile Backend (2026-03 作成) |
| Ludus | 10% | モジュール設計テンプレート集 |
| Educatus | 3% | 用途未確定の placeholder |
| ErgoDLLs | 0% | リポジトリ空 |

### 削除されたリポジトリ

なし。
