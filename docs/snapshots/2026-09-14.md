# LUDIARS Services Progress — 2026-09-14

## 今回の更新

- `docs/data/services.json` と掲載承認の記録を境界として、登録済みの公開61リポジトリを対象に、2026-09-13 03:02 から 2026-09-14 03:02 (Asia/Tokyo) までの local `main` を集計した。
- 59リポジトリを確認でき、3リポジトリに4 commit があった。未マージ branch と worktree 内だけの変更は含めない。
- セッション起動ポリシーの対象解決、Node.js サービス原則、デプロイ差分 API の Git 接続を中心に更新が入った。
- 掲載承認済みの **Orbis / Tela** は「公開予定」として保持した。両者の local `main` は前回確認した commit から更新がなく、実装状況と残件は据え置いた。

## 公開予定のサービス

- **Orbis**：関心ごとにページをグラフで巡るデスクトップブラウザ。アセット / ツールに掲載。
- **Tela**：Pictor を使う再利用可能な C++ 宣言型ツール UI。UI / デザインシステムに掲載。

いずれもリポジトリは公開準備中。GitHub のリンク先は一般公開まで閲覧できない。

### Orbis の進捗

- **実装済み**：既存 Electron 版からの移行先として、Tela/Pictor の操作 UI と CEF/Chromium の Web 表示を組み合わせた Windows ネイティブホストを追加。戻る・進む・再読込と URL 表示を実装した。
- **残作業**：URL 入力、タブ、関心・履歴グラフ、既存データ、用途別設定、外部連携の移行。各 OS の配布とセキュリティ更新追従も完了条件に含む。既存版全体の移行完了ではない。
- **未確認**：IME・スクロール・DPI、終了時の子プロセス解放、待機 CPU/GPU などの実動作受入。本更新では実行していない。
- **根拠**：local `main` の `1873d3f`。対象期間に main の追加 commit はなく、仕様と移行・配布タスクに記録された実装状況・残件を継承した。

### Tela の進捗

- **実装済み**：宣言型 UI ランタイム、入力領域・ジェスチャー所有権、図形描画と対象追従ラベル、Pictor 描画アダプタ、Windows オーバーレイ、Unity Editor 連携と遷移編集サンプル。macOS の AppKit オーバーレイアダプタも main に追加済み。
- **既存の確認記録**：Windows の Release ビルド、4件のライブラリ契約テスト、独立 CMake consumer からの利用、Unity 参照アセンブリでのコンパイル成功が記録されている。本更新で再実行した結果ではない。
- **残作業・未確認**：Mac SDK でのネイティブビルドと実機受入。Windows/Unity の透明表示、入力の共存、再接続、複数画面・DPI、待機負荷と資源解放の確認も残る。Mac アダプタのソース追加を Mac 動作確認済みとは扱わない。
- **根拠**：local `main` の `0a5b7fa`。対象期間に main の追加 commit はなく、既存の実装・検証記録と未完了の受入項目を継承した。

2件の進捗は掲載承認に基づく機能単位の説明であり、公開61件・4 commit の日次集計には含めない。全体の完了条件に対する達成率を算定する根拠がないため、数値の完成度は引き続き未計測。

## 進んだこと

### セッション起動とサービス設計

**Concordia（2 commits）**: 起動時に注入する workflow・必須設定・本体資料を、セッションの最後に登録された作業プロジェクトから解決するよう修正した。未登録の明示対象を起動リポジトリから推測しない境界と、非同期再照合時の古い応答破棄も追加した。あわせて Node.js サービスの採用範囲、責任分離、永続化、再評価条件、受入条件を設計原則として明文化した。

### デプロイ差分の取得

**Revisor（1 commit）**: 登録済みリポジトリのデプロイ差分 API へ既存の Git 実行関数を接続し、HTTP 境界で参照漏れを検出する回帰ケースを追加した。

### ダッシュボード日報

**LUDIARS（1 commit）**: 2026-09-13 のサービスダッシュボード日報と snapshot catalog を追加した。

## 確認した main の変更根拠

| リポジトリ | 件数 | 対象期間の最新 commit |
|---|---:|---|
| Concordia | 2 | [607ccc82](https://github.com/LUDIARS/Concordia/commit/607ccc82) |
| Revisor | 1 | [895a2a8b](https://github.com/LUDIARS/Revisor/commit/895a2a8b) |
| LUDIARS | 1 | [092bd05d](https://github.com/LUDIARS/LUDIARS/commit/092bd05d) |

ローカル main の確認であり、各 commit の GitHub 公開・稼働環境への反映を保証するものではない。

## 完成度と未確認事項

- commit 数は活動量であり、完成条件の達成率ではない。完成度を再評価する定量的根拠は得られていないため、直前のスナップショットにある **72%** と公開対象のリポジトリ別完成度を継承した。72% は過去の評価値で、今回の61件を再集計した割合ではない。
- 未計測のプロジェクトに推測の完成度は追加しない。
- **ErgoDLLs / Omnipotens** は local checkout が見つからず、数値を取得できず。変更なしとは扱わない。残る56リポジトリは対象期間の main に commit がなかった。
- 本更新ではテスト・起動・デプロイを実施していない。実装済みと動作確認済みを区別する。
