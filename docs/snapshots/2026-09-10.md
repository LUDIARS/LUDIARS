# LUDIARS Services Daily Report — 2026-09-10

## 今日の要約

- `docs/data/services.json` に登録済みの公開62リポジトリを対象に、2026-09-09 03:00 から 2026-09-10 03:00 (Asia/Tokyo) までの local `main` を確認した。
- 61リポジトリの local `main` を確認でき、13リポジトリで合計21 commit の更新があった。未マージ branch と worktree 内だけの変更は集計していない。
- Excubitor を中心にサービス共通 Viewer と Villa 資料配信を追加し、Concordia・Praeforma・Peregrinatio を Viewer 内ルーティングへ対応させた。
- Concordia はパートタイマー委託の完了条件と稼働確認を整備し、Revisor は補助タスクのモデル選定を審査モデル強制から分離した。
- Satelles は scheduled worker ごとの Lictor 互換 loopback 窓口を内蔵し、報告・添付・退勤をセッション単位で扱えるようにした。
- 11リポジトリで semver 非破壊範囲の依存更新または lockfile 整合修正が入った。

## ワークストリーム別の変更

### 共通 Viewer・資料配信

- **Excubitor (4 commits)**: サービス共通 Viewer と Villa 資料配信を追加し、catalog に基づく中継、URL 書き換え、Cookie / localStorage のサービス別分離、CSP、WebSocket 経路を整備した。あわせて対象・除外条件、移行・復旧手順、後続タスクを文書化した。
- **Concordia / Praeforma / Peregrinatio (各1 commit)**: Ex Viewer が注入する prefix をルーターの basename として利用し、Viewer 内の画面遷移と従来の直接アクセスを両立させた。

### 委託・レビュー運用

- **Concordia (2 commits)**: パートタイマー委託について、実作業・結果サマリ・配送確認までを完了条件として明確化し、日報・メール・カイゼンの稼働確認タスクを追加した。
- **Revisor (1 commit)**: 補助タスクが審査用の強制モデル設定を継承しないよう分離し、レビュー負荷と空き状況に応じた補助モデル選定、容量不足時の一度だけの切替を追加した。

### セッション専用窓口

- **Satelles (1 commit)**: runner 内にセッション所有の Lictor 互換 loopback sidecar を追加し、子 Codex へ固有の `LICTOR_PORT` / `LICTOR_SESSION_ID` を渡すようにした。報告・会話・task・添付・退勤の宛先をセッション単位に限定し、終了時の資源解放も整備した。

### 依存保守

- **Ars / Synergos / Concordia / Quaestor / Custos / Conciliator / Praeforma / Peregrinatio / Excubitor / Thaleia (各1 commit)**: semver 非破壊範囲の依存更新を反映した。
- **Discutere (1 commit)**: 依存更新後の `package-lock.json` の整合を修正した。

## 集計上の制約

- **ErgoDLLs** は対応する local checkout を確認できなかったため、本日の変更有無は取得できず。変更なしとしては扱わない。
- ダッシュボード上の **Omnipotens** はリポジトリ名に対応する local checkout `Omnipotents` の `main` を確認し、対象期間の commit はなかった。
- 残る48リポジトリは local `main` を確認し、対象期間の commit はなかった。

## 完成度

完成度を変更できる定量的な根拠は今回得られていない。commit 数は活動量であり完成条件の達成率ではないため、`weighted_completion` の **72%** とリポジトリ別完成度は 2026-09-02 の値をそのまま継承した。未計測の公開リポジトリは引き続き完成度一覧へ加えていない。

## 集計ルール

- 公開ダッシュボードの `services.json` に登録済みのリポジトリだけを対象とする。
- 各ローカル checkout の `main` に含まれる変更だけを採用する。
- 未マージ branch、worktree 内だけの変更、private リポジトリは公開日報へ含めない。
- 根拠なしに完成度を増減させない。
